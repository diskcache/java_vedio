create table vedio(
    id int auto_increment primary key,
    -- description text,
    -- uploader varchar(10),
    -- upload_time timestamp
    file_name varchar(50),
    hash varchar(100),
    file_path varchar(50),
    img_path varchar(50)
);
